{% extends 'base.html.twig' %}

{% block body %}
    <h1>New Classroom</h1>

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        
        <button type="submit">Add</button>
    {{ form_end(form) }}
{% endblock %}